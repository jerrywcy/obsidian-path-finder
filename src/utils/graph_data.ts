import { WeightedGraph } from "./../algorithms/graph/weighted_graph";

export class WeightedGraphData {
	edgeMatrix: Array<Array<number>>;
	nodeCount: number;
	edgeCount: number;
	graph: WeightedGraph;
}
